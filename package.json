{
  "name": "vocabmaster",
  "private": true,
  "scripts": {
    "build": "npm run build:css",
    "build:css": "npx tailwindcss -i ./public/style.css -o ./public/tailwind.css --minify",
    "watch:css": "npx tailwindcss -i ./public/style.css -o ./public/tailwind.css --watch",
    "test": "vitest run tests/unit",
    "test:e2e": "playwright test tests/e2e",
    "test:android:emulator": "npx playwright test tests/e2e/android.spec.js",
    "validate": "node tests/tools/check_critical.js && npx playwright test tests/e2e/app.spec.js",
    "validate:critical": "node tests/tools/check_critical.js",
    "// Android asset + build helpers (short-term hygiene win)": "",
    "prepare:android": "npm run build && node -e \"const fs=require('fs'),path=require('path'); const src='public', dst='android/app/src/main/assets'; fs.rmSync(dst,{recursive:true,force:true}); fs.mkdirSync(dst,{recursive:true}); fs.cpSync(src,dst,{recursive:true}); console.log('✅ Synced',src,'→',dst) \"",
    "sync:android": "npm run prepare:android",
    "clean:android": "node -e \"const fs=require('fs'); ['android/app/build','android/.gradle','.gradle','android/app/src/main/assets'].forEach(p => { try{fs.rmSync(p,{recursive:true,force:true});console.log('🧹 cleaned',p)}catch(e){} }); console.log('Android build caches cleared (run sync:android next)')\"",
    "prebuild:android": "npm run validate",
    "build:android": "npm run prepare:android && cd android && gradlew clean assembleDebug --no-daemon",
    "test:runtime:emulator": "bash scripts/verify-runtime-emulator.sh",
    "audit": "node tests/audit/grammar_audit.mjs",
    "feedback": "node tests/audit/grammar_feedback.mjs",
    "feedback:summary": "node tests/audit/grammar_feedback.mjs --summary",
    "regenerate": "node tests/audit/grammar_feedback.mjs --regenerate",
    "prepare": "husky",
    "dev": "vite",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "husky": "^9.1.7",
    "tailwindcss": "^3",
    "vite": "^8.0.16",
    "vitest": "^4.1.4"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.20"
  }
}
